
  something(element)(vSomething)(vNothing) {
    vSomething(element)
  }

  nothing(vSomething)(vNothing) {
    vNothing
  }


  default(defaultValue)(maybe) {
    maybe
      ((element){ element })
      (defaultValue)
  }
