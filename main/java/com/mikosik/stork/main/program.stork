
  build {
    module
      (import("stork.opcode.opArg"))
  }

  writeStream(stream) {
    stream
      ((head)(tail) {
        opArg(writeByte)(head)(writeStream(tail))
      })
      (writeByte(-1))
  }

  writeByte(byte)(continue) {
    continue
  }
