
  build {
    module
      (exportAs("stork.function.self")("self"))
      (exportAs("stork.function.compose")("compose"))
      (exportAs("stork.function.flip")("flip"))
  }

  self(x) {
    x
  }

  compose(f)(g)(x) {
    f(g(x))
  }

  flip(f)(x)(y) {
    f(y)(x)
  }
