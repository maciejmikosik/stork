
  build {
    module
      (import("stork.integer.equal"))
      (import("stork.integer.negate"))
  }

  lazy(number) {
    number
  }

  main {
    equal(negate(lazy(2)))(lazy(-2))
      ("true")
      ("false")
  }
